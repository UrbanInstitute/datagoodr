% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03-03-granular.R
\name{dg_element}
\alias{dg_element}
\alias{dg_stats}
\alias{dg_properties}
\alias{dg_quantiles}
\alias{dg_preview}
\alias{dg_levels}
\alias{dg_graphic}
\alias{dg_field}
\title{Render one element of a variable's profile}
\usage{
dg_stats(dgf, var_name, label = "STATS")

dg_properties(dgf, var_name, label = "PROPERTIES")

dg_quantiles(dgf, var_name, label = "QUANTILES")

dg_preview(dgf, var_name, label = "PREVIEW", size = 80)

dg_levels(dgf, var_name, label = "LEVELS")

dg_graphic(dgf, var_name, label = NULL)

dg_field(dgf, var_name, field, label = toupper(field))
}
\arguments{
\item{dgf}{A DGF data frame.}

\item{var_name}{The variable name (a value of `dgf$var_name`).}

\item{label}{Optional section label; defaults to the render function's own
default (e.g. `"STATS"`).}

\item{size}{Maximum line width for `dg_preview`, in characters (default 80).}

\item{field}{DGF column name to print (for `dg_field`).}
}
\value{
No return value; prints the element. Call inside a
  `results = "asis"` chunk.
}
\description{
Print a single piece of a variable's profile inline: its summary statistics,
data-quality properties, value preview, factor levels, graphic, or any text
field from the DGF.
}
\section{Functions}{
\itemize{
\item \code{dg_stats()}: Summary statistics (type-appropriate).

\item \code{dg_properties()}: Data-quality properties (rows, distinct, missing, ...).

\item \code{dg_quantiles()}: Quantile table (numeric variables).

\item \code{dg_preview()}: Preview of example values (numeric or character).

\item \code{dg_levels()}: Factor/logical level dictionary.

\item \code{dg_graphic()}: The type-appropriate graphic (histogram, treemap,
bar plot, or word cloud). Set chunk `fig.width`/`fig.height` to size it.

\item \code{dg_field()}: Any text field from the DGF (e.g. `"dd_vdesc"`,
`"dd_vlabel"`, `"dd_vdesc"`). `field` is the DGF column name.

}}
\seealso{
[dg_section()]
}
