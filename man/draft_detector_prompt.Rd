% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-llm-prompts.R
\name{draft_detector_prompt}
\alias{draft_detector_prompt}
\title{Draft a copy-paste prompt asking an LLM to write a new type detector}
\usage{
draft_detector_prompt(
  examples,
  type_name,
  n_negatives = 15,
  seed = 514,
  file = NULL,
  contract_path = NULL,
  quiet = FALSE
)
}
\arguments{
\item{examples}{A character vector of 10-25 valid example values for the new
type.}

\item{type_name}{A single lower_snake_case type name, e.g. `"iso_country"`.
The LLM is asked to write `is_<type_name>()`.}

\item{n_negatives}{Max hard negatives to include per mutation tier (keeps the
prompt readable; the file still captures everything you pass).}

\item{seed}{Integer seed for reproducible test-case generation.}

\item{file}{Output path for the prompt `.md`; `NULL` writes to `tempdir()`.}

\item{contract_path}{Optional override for the bundled contract (path or URL).}

\item{quiet}{If `TRUE`, only write the file (no console print).}
}
\value{
The prompt file path, invisibly. The prompt text is printed unless
  `quiet = TRUE`.
}
\description{
Turns a handful of example values for a new data type into a self-contained
prompt: the versioned detector contract (see
`inst/prompts/detector-contract.md`) followed by the type's positive
examples and machine-generated hard negatives. Paste or attach the result
into any LLM; feed the function it returns to [evaluate_detector()].
}
\seealso{
[evaluate_detector()], [guess_data_type()]
}
