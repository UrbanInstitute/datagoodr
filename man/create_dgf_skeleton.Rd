% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01-01-dgf-schema.R
\name{create_dgf_skeleton}
\alias{create_dgf_skeleton}
\title{Create an empty v2 DGF skeleton}
\usage{
create_dgf_skeleton(varnames)
}
\arguments{
\item{varnames}{Character vector of variable names from the raw source.}
}
\value{
A data frame with one row per variable and every DGF column, all
  blank except `var_name`.
}
\description{
Builds an empty Data Governance File: one row per variable, every schema
column present. `create_dgf()` populates it; call this directly only to see
the schema or build a DGF by hand.
}
\details{
Columns are grouped by prefix family — `dd_`, `raw_`, `stable_`,
  `rg_`, `validate_`, `prov_`. The `rg_*` columns are character (they hold
  JSON), not list-columns, so the DGF survives an `.xlsx` round trip.
}
\seealso{
[create_dgf()], [inspect_dgf()]
}
